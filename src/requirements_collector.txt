# SentinelCore — Windows Collector Dependencies
# Install on each of the 100+ Windows PCs running collector.py
#
# Usage (PowerShell as Administrator):
#   pip install -r requirements_collector.txt
#
# Components running on Windows PCs:
#   collector.py — reads Windows Event Logs, sends batches to Kafka

# ── Windows Event Log API (Windows-only) ─────────────────────────────────────
pywin32>=311

# ── System metrics ────────────────────────────────────────────────────────────
psutil>=7.2.2

# ── HTTP client (health-check fallback) ──────────────────────────────────────
requests>=2.32.5

# ── Kafka producer ────────────────────────────────────────────────────────────
kafka-python-ng>=2.2.3

# NOTE: psycopg2 is intentionally NOT here.
# collector.py sends events to Kafka and never connects to the database directly.
# The database lives on the central server only.